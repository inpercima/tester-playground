overwrite: true
schema: "https://api.angular.schule/graphql"
documents: "src/**/*.graphql"
generates:
  src/graphql/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
